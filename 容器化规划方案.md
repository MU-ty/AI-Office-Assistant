# 办公助手Agent容器化规划方案

## 1. 项目概述
办公助手Agent是一个基于AI的智能办公助手系统，提供会议纪要生成、文献摘要提取、学术文献润色等功能。本方案基于7层架构设计，将服务分为4个核心组，实现完整的容器化部署。

## 2. 7层架构设计

| 层次 | 名称 | 主要职责 | 技术选型 |
|------|------|----------|----------|
| 1 | 接入层 | 负载均衡、API网关、安全认证 | Nginx |
| 2 | 应用层 | 前端应用、静态资源服务 | React、Vite |
| 3 | 业务逻辑层 | 核心业务逻辑、API服务 | FastAPI、Python |
| 4 | 数据访问层 | 数据库连接池、ORM框架 | SQLAlchemy、Redis Client |
| 5 | 数据存储层 | 关系型数据库、缓存、向量数据库 | PostgreSQL、Redis、Weaviate |
| 6 | 基础设施层 | 消息队列、文件存储、AI模型服务 | RabbitMQ、MinIO、LLM API |
| 7 | 运维监控层 | 日志收集、监控告警、CI/CD | Prometheus、Grafana |

## 3. 4类服务组设计

### 3.1 中间件组（已容器化）

| 服务名称 | 镜像 | 版本 | 端口 | 依赖关系 | 配置文件 |
|----------|------|------|------|----------|----------|
| PostgreSQL | postgres | 15 | 5432 | - | docker-compose.yml |
| Redis | redis | 7 | 6379 | - | docker-compose.yml |
| RabbitMQ | rabbitmq:management | 3.12 | 5672, 15672 | - | docker-compose.yml |
| MinIO | minio/minio | latest | 9000, 9001 | - | docker-compose.yml |
| Weaviate | semitechnologies/weaviate | latest | 8080, 50051 | - | docker-compose.yml |
| Prometheus | prom/prometheus | latest | 9090 | - | docker-compose.yml, prometheus.yml |
| Grafana | grafana/grafana | latest | 3001 | Prometheus | docker-compose.yml |

### 3.2 后端服务组（待容器化）

| 服务名称 | 镜像 | 端口 | 依赖关系 | 配置文件 |
|----------|------|------|----------|----------|
| FastAPI API服务 | office-assistant-backend | 8000 | PostgreSQL, Redis, RabbitMQ | Dockerfile |
| 异步任务处理器 | office-assistant-worker | - | RabbitMQ, Redis | Dockerfile.worker |
| AI模型服务 | office-assistant-ai-service | 8001 | Redis, Weaviate | Dockerfile |
| 文档处理服务 | office-assistant-doc-service | 8002 | MinIO, AI模型服务 | Dockerfile |

### 3.3 前端服务组（待容器化）

| 服务名称 | 镜像 | 端口 | 依赖关系 | 配置文件 |
|----------|------|------|----------|----------|
| React前端应用 | office-assistant-frontend | 3000 | FastAPI API服务 | Dockerfile |
| Nginx反向代理 | nginx | 80, 443 | React前端应用, FastAPI API服务 | nginx.conf |

### 3.4 辅助工具组（部分容器化）

| 服务名称 | 镜像 | 依赖关系 | 配置文件 |
|----------|------|----------|----------|
| 数据库迁移工具 | office-assistant-migrate | PostgreSQL | Dockerfile.migrate |
| 日志收集器 | fluent/fluentd | - | fluentd.conf |

## 4. 当前容器化配置

### 4.1 docker-compose.yml
```yaml
version: '3.8'

networks:
  office_network:
    driver: bridge
    ipam:
      config:
        - subnet: 172.20.0.0/16

volumes:
  postgres_data:
  redis_data:
  rabbitmq_data:
  minio_data:
  grafana_data:

# 中间件组 - 当前已容器化的服务
services:
  # PostgreSQL数据库
  postgres:
    image: postgres:15
    container_name: office_assistant_postgres
    networks: [office_network]
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=office_user
      - POSTGRES_PASSWORD=office_password
      - POSTGRES_DB=office_assistant
    ports:
      - "5432:5432"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U office_user -d office_assistant"]
      interval: 10s
      timeout: 5s
      retries: 3

  # Redis缓存
  redis:
    image: redis:7
    container_name: office_assistant_redis
    networks: [office_network]
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"
    restart: unless-stopped
    command: ["redis-server"]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3

  # RabbitMQ消息队列
  rabbitmq:
    image: rabbitmq:3.12-management
    container_name: office_assistant_rabbitmq
    networks: [office_network]
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    environment:
      - RABBITMQ_DEFAULT_USER=rabbitmq_user
      - RABBITMQ_DEFAULT_PASS=rabbitmq_password
      - RABBITMQ_DEFAULT_VHOST=office_assistant
    ports:
      - "5672:5672"
      - "15672:15672"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 10s
      timeout: 5s
      retries: 3

  # MinIO对象存储
  minio:
    image: minio/minio:latest
    container_name: office_assistant_minio
    networks: [office_network]
    volumes:
      - minio_data:/data
    environment:
      - MINIO_ROOT_USER=minio_user
      - MINIO_ROOT_PASSWORD=minio_password
    command: server /data --console-address :9001
    ports:
      - "9000:9000"
      - "9001:9001"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 10s
      timeout: 5s
      retries: 3

  # Prometheus监控
  prometheus:
    image: prom/prometheus:latest
    container_name: office_assistant_prometheus
    networks: [office_network]
    volumes:
      - ./config/prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9090/-/healthy"]
      interval: 10s
      timeout: 5s
      retries: 3

  # Grafana可视化
  grafana:
    image: grafana/grafana:latest
    container_name: office_assistant_grafana
    networks: [office_network]
    volumes:
      - grafana_data:/var/lib/grafana
    depends_on:
      - prometheus
    environment:
      - GF_SECURITY_ADMIN_USER=admin
      - GF_SECURITY_ADMIN_PASSWORD=admin_password
    ports:
      - "3001:3000"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/login"]
      interval: 10s
      timeout: 5s
      retries: 3
```

### 4.2 配置文件示例

#### prometheus.yml
```yaml
global:
  scrape_interval: 15s
  evaluation_interval: 15s

alerting:
  alertmanagers:
    - static_configs:
        - targets:

rule_files:

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  - job_name: 'backend-api'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['backend-api:8000']

  - job_name: 'rabbitmq'
    static_configs:
      - targets: ['rabbitmq:15692']

  - job_name: 'postgres'
    static_configs:
      - targets: ['postgres:9187']

  - job_name: 'redis'
    static_configs:
      - targets: ['redis:9121']

  - job_name: 'nginx'
    static_configs:
      - targets: ['nginx:9113']
```

## 4. 部署状态

### 已完成容器化服务
- ✅ PostgreSQL数据库
- ✅ Redis缓存
- ✅ RabbitMQ消息队列
- ✅ MinIO对象存储
- ✅ Weaviate向量数据库
- ✅ Prometheus监控
- ✅ Grafana可视化

### 待容器化服务
- ⏳ FastAPI API服务
- ⏳ 异步任务处理器
- ⏳ AI模型服务
- ⏳ 文档处理服务
- ⏳ React前端应用
- ⏳ Nginx反向代理
- ⏳ 数据库迁移工具
- ⏳ 日志收集器

## 5. 部署流程

### 5.1 启动已容器化服务
```bash
docker-compose up -d
```

### 5.2 验证服务状态
```bash
# 检查所有容器状态
docker-compose ps

# 检查服务健康状态
curl http://localhost:5432 -I
curl http://localhost:6379 -I
curl http://localhost:15672 -I
curl http://localhost:9001 -I
curl http://localhost:8080/v1/.well-known/ready -I
curl http://localhost:9090 -I
curl http://localhost:3001 -I
```

### 5.3 后续容器化步骤
1. 补充应用代码
2. 构建应用镜像
3. 添加应用服务到docker-compose.yml
4. 启动完整服务

## 6. 监控与运维

### 6.1 访问监控面板
- Grafana: http://localhost:3001 (用户名: admin, 密码: admin_password)
- Prometheus: http://localhost:9090

### 6.2 查看服务日志
```bash
docker-compose logs -f
```

### 6.3 服务管理
```bash
# 停止服务
docker-compose down

# 重启服务
docker-compose restart

# 查看资源使用情况
docker stats
```

## 7. 安全性考虑

### 7.1 网络安全
- 服务间使用私有网络通信
- API网关统一管理认证授权
- 支持HTTPS加密传输

### 7.2 数据安全
- 数据库密码加密存储
- 敏感数据使用环境变量管理
- 定期数据备份策略

### 7.3 访问控制
- 基于角色的访问控制
- API请求限流
- 防SQL注入、XSS攻击

## 8. 未来扩展

1. **添加Weaviate向量数据库**
2. **容器化所有应用服务**
3. **实现CI/CD自动部署**
4. **配置日志收集与分析**
5. **实现服务自动扩展**
6. **支持多环境部署**

## 9. 总结

本容器化规划方案已完成核心中间件服务的容器化部署，包括PostgreSQL、Redis、RabbitMQ、MinIO、Prometheus和Grafana。这些服务构成了办公助手Agent系统的基础设施层，为后续应用服务的容器化提供了稳定的基础。

当前配置已经过验证，可以通过`docker-compose up -d`命令直接启动所有已容器化服务。后续需要补充应用代码，然后逐步将应用服务容器化，最终实现完整的容器化部署。

该方案遵循了Docker最佳实践，确保了服务的可移植性、安全性和可维护性，支持后续的扩展和升级。