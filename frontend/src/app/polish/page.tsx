import PolishModule from "@/modules/polish";

export default function PolishPage() {
  return (
    <div className="h-[calc(100vh-4rem)] w-full p-4 bg-slate-50">
      <PolishModule />
    </div>
  );
}
