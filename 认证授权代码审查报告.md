# 认证授权代码审查报告

## 📋 审查日期

2026年1月23日

---

## 🔴 发现的问题及修复

### **问题 1: JWT Token 的 `exp` 字段类型错误** ⭐ **严重**

**位置**: [backend/app/auth/jwt.py](backend/app/auth/jwt.py)

**问题描述**:

- JWT 标准要求 `exp` (expiration time) 必须是 **Unix 时间戳（秒）**，而不是 datetime 对象
- 原代码直接将 datetime 对象放入 token payload，会导致 token 编码失败或验证失败

**原代码**:

```python
to_encode.update({"exp": expire})  # ❌ expire 是 datetime 对象
```

**修复后**:

```python
to_encode.update({"exp": int(expire.timestamp())})  # ✅ 转换为 Unix 时间戳
```

**影响范围**:

- `create_access_token()` 函数
- `create_refresh_token()` 函数

---

### **问题 2: 权限检查逻辑完全错误** ⭐ **严重**

**位置**: [backend/app/auth/dependencies.py](backend/app/auth/dependencies.py#L100)

**问题描述**:

- 权限检查使用了错误的查询逻辑
- 错误地在 `User` 表上查询，条件为 `User.id == permission`（逻辑不通）
- 应该查询 `UserPermission` 表

**原代码**:

```python
user_permission = db.query(User).filter(
    User.id == current_user.id,
    User.id == permission,  # ❌ 这个条件完全错误！
).first()
```

**修复后**:

```python
from app.db.models import UserPermission

user_permission = db.query(UserPermission).filter(
    UserPermission.user_id == current_user.id,
    UserPermission.permission == permission,  # ✅ 正确的字段
).first()
```

**影响范围**:

- `require_permission()` 装饰器工厂函数
- 所有使用权限验证的 API 端点都会失效

---

### **问题 3: 导入问题** ⭐ **中等**

**位置**: [backend/app/auth/**init**.py](backend/app/auth/__init__.py)

**问题描述**:

- 缺少 `dependencies.py` 中的依赖函数导出
- 导致外部无法直接从 `app.auth` 导入 `get_current_user` 等依赖

**修复前**:

```python
from .password import ...
from .jwt import ...
# 缺少 from .dependencies import ...
```

**修复后**:

```python
from .password import ...
from .jwt import ...
from .dependencies import get_current_user, get_current_admin_user, require_permission

__all__ = [
    ...,
    "get_current_user",
    "get_current_admin_user",
    "require_permission",
]
```

---

### **问题 4: 重复导入** ⭐ **轻微**

**位置**: [backend/app/api/routes/auth.py](backend/app/api/routes/auth.py)

**问题描述**:

- `datetime` 在文件开头已导入，但在函数中又重新导入

**原代码**:

```python
from datetime import timedelta  # 开头导入

# ...

def login(...):
    from datetime import datetime  # ❌ 重复导入
    db_user.last_login_at = datetime.now()
```

**修复后**:

```python
from datetime import timedelta, datetime  # 合并导入

def login(...):
    db_user.last_login_at = datetime.now()  # ✅ 直接使用
```

---

## ✅ 修复总结

| 问题             | 严重性  | 状态      |
| ---------------- | ------- | --------- |
| JWT exp 字段类型 | 🔴 严重 | ✅ 已修复 |
| 权限检查逻辑     | 🔴 严重 | ✅ 已修复 |
| 导入缺失         | 🟡 中等 | ✅ 已修复 |
| 重复导入         | 🟢 轻微 | ✅ 已修复 |

---

## 🧪 建议的测试清单

### 1. JWT Token 验证测试

```bash
# 测试 token 是否能正确编码和解码
python -m pytest backend/tests/test_jwt.py -v
```

### 2. 认证流程测试

```bash
# 测试完整的登录流程
python -m pytest backend/tests/test_auth.py -v
```

### 3. 权限检查测试

```bash
# 测试权限验证是否工作正常
python -m pytest backend/tests/test_permissions.py -v
```

### 4. 集成测试

```bash
# 使用 curl 或 Postman 测试 API 端点
curl -X POST http://localhost:8000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"test_user","password":"TestPass123"}'
```

---

## 📌 代码质量建议

### 1. 添加类型提示

```python
# 改进前
def verify_user(...):
    ...

# 改进后
from typing import Optional
from app.db.models import User

def verify_user(user_id: str, db: Session) -> Optional[User]:
    ...
```

### 2. 添加单元测试

- 为每个认证函数编写单元测试
- 使用 `pytest` 和 `pytest-asyncio` 进行异步测试

### 3. 错误处理

- 统一错误响应格式
- 避免暴露敏感信息（如数据库错误）

### 4. 安全建议

- ✅ 密码已使用 bcrypt 加密（好）
- ✅ JWT 使用 HS256 算法（好）
- 📝 建议定期轮换 JWT_SECRET_KEY
- 📝 建议实现 token 黑名单（logout）
- 📝 建议添加登录尝试次数限制

---

## 🚀 下一步行动

1. **运行测试** - 验证修复是否有效
2. **集成前端** - 确保前端能正确处理 token 和错误响应
3. **安全审计** - 定期进行安全检查
4. **监控日志** - 添加认证相关的日志记录
