# Docker数据迁移说明

## 核心结论
**docker-compose.yml 文件本身不包含任何实际数据**，它只定义了服务的配置结构（包括数据卷的使用方式）。因此，在另一台电脑上运行 `docker-compose up` 时：

- 只会**创建新的空数据卷**
- 不会自动携带原电脑上的任何数据
- 需要手动进行数据迁移操作

## 数据卷类型与迁移差异

### 1. 命名卷（当前配置使用）
- **特点**：由 Docker 引擎管理，存储在 Docker 默认目录 `/var/lib/docker/volumes/` 中
- **迁移方式**：需要手动备份和恢复数据

### 2. 绑定挂载（Host Volumes）
- **特点**：直接挂载宿主机目录到容器
- **迁移方式**：可以通过复制宿主机目录实现数据迁移

### 3. 匿名卷
- **特点**：自动生成随机名称，无明确管理
- **迁移方式**：不建议用于需要持久化的数据

## 当前配置的数据迁移方案

### PostgreSQL 数据迁移
```bash
# 备份（原电脑）
docker exec -t office_assistant_postgres pg_dump -U office_user office_assistant > backup.sql

# 恢复（新电脑，先启动容器）
docker exec -i office_assistant_postgres psql -U office_user office_assistant < backup.sql
```

### Redis 数据迁移
```bash
# 备份（原电脑）
docker exec -t office_assistant_redis redis-cli SAVE
docker cp office_assistant_redis:/data/dump.rdb ./redis_backup.rdb

# 恢复（新电脑）
docker cp ./redis_backup.rdb office_assistant_redis:/data/dump.rdb
docker restart office_assistant_redis
```

### MinIO 数据迁移
```bash
# 备份（原电脑）
docker run --rm -v ai-office-assistant_minio_data:/data -v $(pwd):/backup alpine tar -czf /backup/minio_backup.tar.gz /data

# 恢复（新电脑）
docker run --rm -v ai-office-assistant_minio_data:/data -v $(pwd):/backup alpine tar -xzf /backup/minio_backup.tar.gz -C /
```

### RabbitMQ 数据迁移
```bash
# 备份（原电脑）
docker exec -t office_assistant_rabbitmq rabbitmqctl export_definitions /tmp/rabbitmq_definitions.json
docker cp office_assistant_rabbitmq:/tmp/rabbitmq_definitions.json ./rabbitmq_definitions.json

# 恢复（新电脑）
docker cp ./rabbitmq_definitions.json office_assistant_rabbitmq:/tmp/rabbitmq_definitions.json
docker exec -t office_assistant_rabbitmq rabbitmqctl import_definitions /tmp/rabbitmq_definitions.json
```

### Grafana 数据迁移
```bash
# 备份（原电脑）
docker run --rm -v ai-office-assistant_grafana_data:/data -v $(pwd):/backup alpine tar -czf /backup/grafana_backup.tar.gz /data

# 恢复（新电脑）
docker run --rm -v ai-office-assistant_grafana_data:/data -v $(pwd):/backup alpine tar -xzf /backup/grafana_backup.tar.gz -C /
```

### Weaviate 数据迁移
```bash
# 备份（原电脑）
docker run --rm -v ai-office-assistant_weaviate_data:/data -v $(pwd):/backup alpine tar -czf /backup/weaviate_backup.tar.gz /data

# 恢复（新电脑）
docker run --rm -v ai-office-assistant_weaviate_data:/data -v $(pwd):/backup alpine tar -xzf /backup/weaviate_backup.tar.gz -C /
```

#### 使用 Weaviate 导出/导入 API（推荐）
```bash
# 导出所有数据（原电脑）
curl -X POST http://localhost:8080/v1/backups/export -H "Content-Type: application/json" -d '{"includeData": true}'

# 等待导出完成
curl -X GET http://localhost:8080/v1/backups/export

# 下载导出文件
docker cp office_assistant_weaviate:/var/lib/weaviate/backups/export.tar.gz ./weaviate_export.tar.gz

# 上传导出文件到新电脑
scp ./weaviate_export.tar.gz user@new-server:/path/to/destination/

# 在新电脑上导入数据
docker cp /path/to/destination/weaviate_export.tar.gz office_assistant_weaviate:/var/lib/weaviate/backups/

# 执行导入
curl -X POST http://localhost:8080/v1/backups/import -H "Content-Type: application/json" -d '{"backupFile": "/var/lib/weaviate/backups/export.tar.gz"}'
```

## 简化的数据迁移方案

### 使用 Docker 卷导出/导入（推荐）
```bash
# 导出卷（原电脑）
docker volume export ai-office-assistant_postgres_data > postgres_data.tar

# 导入卷（新电脑）
docker volume create ai-office-assistant_postgres_data
docker run --rm -i -v ai-office-assistant_postgres_data:/target alpine tar -xzf - -C /target < postgres_data.tar
```

### 直接复制卷文件（适合同操作系统）
```bash
# 在原电脑上找到卷路径
docker volume inspect ai-office-assistant_postgres_data
# 复制输出中的 Mountpoint 路径下的所有文件到新电脑

# 在新电脑上停止服务
docker-compose down
# 复制文件到新电脑的对应卷路径
docker-compose up -d
```

## 注意事项

1. **版本兼容性**：确保新电脑上的 Docker 版本与原电脑兼容
2. **操作系统差异**：Windows 和 Linux 之间的数据卷文件格式可能不同，建议使用跨平台的备份方法
3. **服务状态**：迁移数据前确保服务已停止或处于稳定状态
4. **密码一致性**：确保新电脑上的环境变量（尤其是密码）与原电脑一致
5. **测试验证**：迁移后务必测试数据完整性和服务可用性

## 自动化迁移建议

对于频繁的数据迁移需求，可以考虑：
- 编写自动化脚本处理备份和恢复流程
- 使用云存储服务（如 S3）作为中间存储
- 实现定期自动备份机制
- 考虑使用数据库主从复制架构

## 总结

docker-compose.yml 是**配置文件**，而非**数据文件**。跨电脑部署时，必须：
1. 先在新电脑上启动基础服务
2. 然后通过合适的方式迁移数据
3. 最后验证数据完整性

通过正确的数据迁移流程，可以确保服务在不同环境中保持一致的数据状态。