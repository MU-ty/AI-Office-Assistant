# AI办公助手容器化操作指南

## 1. 项目概述

本项目采用Docker容器化部署，包含以下核心服务：

| 服务类型 | 服务名称 | 版本 | 主要功能 |
|---------|---------|------|---------|
| 数据库 | PostgreSQL | 15 | 关系型数据库存储 |
| 缓存 | Redis | 7 | 缓存服务 |
| 消息队列 | RabbitMQ | 3.12 | 消息传递和任务队列 |
| 对象存储 | MinIO | latest | 分布式对象存储 |
| 向量数据库 | Weaviate | latest | AI向量存储和检索 |
| 监控 | Prometheus | latest | 监控指标收集 |
| 日志 | Loki | latest | 日志收集系统 |
| 日志代理 | Promtail | latest | 日志收集代理 |
| 可视化 | Grafana | latest | 监控和日志可视化 |

## 2. 中间件用途详解

### 2.1 PostgreSQL
PostgreSQL是一个强大的开源关系型数据库，在本项目中主要用于：
- 存储系统配置信息
- 存储用户数据和权限信息
- 存储业务数据和状态信息
- 支持复杂的SQL查询和事务处理

### 2.2 Redis
Redis是一个高性能的内存数据库，在本项目中主要用于：
- 缓存热点数据，提高系统响应速度
- 存储会话信息和临时数据
- 实现分布式锁，保证数据一致性
- 作为消息队列，处理轻量级的异步任务

### 2.3 RabbitMQ
RabbitMQ是一个可靠的消息队列系统，在本项目中主要用于：
- 实现服务间的异步通信
- 处理后台任务和定时任务
- 实现系统解耦，提高系统可靠性
- 支持消息持久化和确认机制

### 2.4 MinIO
MinIO是一个高性能的对象存储服务，在本项目中主要用于：
- 存储上传的文档和文件
- 存储AI模型生成的结果和中间数据
- 提供高可用和可扩展的存储解决方案
- 兼容S3 API，便于与其他系统集成

### 2.5 Weaviate
Weaviate是一个向量数据库，专为AI应用设计，在本项目中主要用于：
- 存储和检索向量数据
- 支持相似性搜索和语义搜索
- 为AI模型提供高效的向量检索服务
- 支持多种向量距离计算方法

### 2.6 Prometheus
Prometheus是一个开源的监控系统，在本项目中主要用于：
- 收集和存储系统和服务的监控指标
- 支持多维度的指标查询和分析
- 实现基于指标的告警功能
- 与Grafana集成，提供可视化监控

### 2.7 Loki
Loki是一个开源的日志聚合系统，在本项目中主要用于：
- 收集和存储所有服务的日志数据
- 支持高效的日志查询和过滤
- 与Promtail配合，实现日志收集
- 与Grafana集成，提供日志可视化

### 2.8 Promtail
Promtail是一个日志代理，在本项目中主要用于：
- 从Docker容器中收集日志
- 将日志发送到Loki进行存储
- 支持日志标签和过滤
- 实现高效的日志传输

### 2.9 Grafana
Grafana是一个开源的可视化平台，在本项目中主要用于：
- 展示Prometheus收集的监控指标
- 提供Loki日志查询和可视化
- 支持创建自定义仪表盘
- 实现告警和通知功能

## 3. 环境准备

### 3.1 系统要求
- Docker 20.10+ 
- Docker Compose 2.0+ 
- 至少4GB内存
- 至少20GB磁盘空间

### 3.2 安装Docker
- **Windows**：安装Docker Desktop for Windows
- **macOS**：安装Docker Desktop for Mac
- **Linux**：
  ```bash
  curl -fsSL https://get.docker.com -o get-docker.sh
  sudo sh get-docker.sh
  ```

## 4. Docker Compose 操作

### 4.1 启动服务
```bash
# 启动所有服务（后台运行）
docker-compose up -d

# 启动特定服务
docker-compose up -d postgres redis

# 查看启动过程日志
docker-compose up
```

### 4.2 停止服务
```bash
# 停止所有服务
docker-compose down

# 停止特定服务
docker-compose stop postgres redis

# 停止并删除数据卷
docker-compose down -v
```

### 4.3 查看服务状态
```bash
# 查看所有服务状态
docker-compose ps

# 查看服务日志
docker-compose logs

# 实时跟踪特定服务日志
docker-compose logs -f postgres
```

### 4.4 服务管理
```bash
# 重启所有服务
docker-compose restart

# 重启特定服务
docker-compose restart postgres

# 检查服务健康状态
docker-compose ps --filter health=healthy
```

## 5. 服务访问

### 5.1 主要服务访问地址

| 服务 | 访问地址 | 用户名 | 密码 |
|------|---------|--------|------|
| PostgreSQL | localhost:5432 | office_user | office_password |
| Redis | localhost:6379 | - | - |
| RabbitMQ Management | http://localhost:15672 | rabbitmq_user | rabbitmq_password |
| MinIO Console | http://localhost:9001 | minio_user | minio_password |
| MinIO API | http://localhost:9000 | minio_user | minio_password |
| Weaviate API | http://localhost:8080 | - | - |
| Prometheus | http://localhost:9090 | - | - |
| Grafana | http://localhost:3001 | admin | admin_password |
| Loki | http://localhost:3100 | - | - |

### 5.2 服务连接示例

#### PostgreSQL连接示例
```bash
# 使用psql连接
psql -h localhost -p 5432 -U office_user -d office_assistant

# 使用Docker连接容器内PostgreSQL
docker exec -it office_assistant_postgres psql -U office_user office_assistant
```

#### Redis连接示例
```bash
# 使用redis-cli连接
redis-cli -h localhost -p 6379

# 使用Docker连接容器内Redis
docker exec -it office_assistant_redis redis-cli
```

#### Weaviate连接示例
```bash
# 使用curl测试Weaviate连接
curl http://localhost:8080/v1/.well-known/ready

# 使用Python客户端连接
# 安装客户端: pip install weaviate-client
python -c "
import weaviate
client = weaviate.Client(url='http://localhost:8080')
print(client.is_ready())
"

# 使用Docker连接容器内Weaviate
# 注意：Weaviate容器内没有curl命令，使用wget替代
# docker exec -it office_assistant_weaviate wget -qO- http://localhost:8080/v1/.well-known/ready

# 外部验证Weaviate服务状态
curl http://localhost:8080/v1/.well-known/ready
```

#### MinIO连接示例
```bash
# 使用MinIO客户端(mc)连接
# 安装客户端: https://min.io/docs/minio/linux/reference/minio-mc.html
mc config host add local http://localhost:9000 minio_user minio_password

# 列出存储桶
mc ls local

# 使用Python客户端连接
# 安装客户端: pip install minio
python -c "
from minio import Minio
client = Minio(
    'localhost:9000',
    access_key='minio_user',
    secret_key='minio_password',
    secure=False
)
print(client.list_buckets())
"

# 使用Docker连接容器内MinIO
docker exec -it office_assistant_minio curl -s http://localhost:9000/minio/health/live
```

#### RabbitMQ AMQP连接示例
```bash
# 使用Python pika库连接
# 安装客户端: pip install pika
python -c "
import pika
credentials = pika.PlainCredentials('rabbitmq_user', 'rabbitmq_password')
connection = pika.BlockingConnection(
    pika.ConnectionParameters('localhost', 5672, '/', credentials)
)
channel = connection.channel()
print('RabbitMQ connected successfully')
connection.close()
"

# 使用Docker连接容器内RabbitMQ AMQP
# 在已运行的容器中执行状态检查
docker exec -it office_assistant_rabbitmq rabbitmqctl status
```

#### Prometheus连接示例
```bash
# 使用curl访问Prometheus API
curl http://localhost:9090/api/v1/query?query=up

# 使用Python客户端连接
# 安装客户端: pip install prometheus-api-client
python -c "
from prometheus_api_client import PrometheusConnect
prom = PrometheusConnect(url='http://localhost:9090', disable_ssl=True)
print(prom.get_current_metric_value(metric_name='up'))
"

# 使用Docker连接容器内Prometheus
# 注意：Prometheus容器内没有curl命令，使用wget替代
# docker exec -it office_assistant_prometheus wget -qO- http://localhost:9090/-/healthy

# 外部验证Prometheus服务状态
curl http://localhost:9090/-/healthy
```

#### Grafana连接示例
```bash
# 使用curl访问Grafana API
# 先获取API令牌: http://localhost:3001/org/apikeys
curl -H "Authorization: Bearer YOUR_API_TOKEN" http://localhost:3001/api/dashboards/home

# 使用Python客户端连接
# 安装客户端: pip install grafana-api
python -c "
from grafana_api.grafana_face import GrafanaFace
grafana = GrafanaFace(auth=('admin', 'admin_password'), host='localhost:3001', protocol='http')
print(grafana.org.get_org())
"

# 使用Docker连接容器内Grafana
docker exec -it office_assistant_grafana curl -s http://localhost:3000/api/health
```

#### Loki连接示例
```bash
# 使用curl发送日志到Loki
curl -X POST -H "Content-Type: application/json" -d '{
  "streams": [
    {
      "stream": {"job": "example"},
      "values": [["'$(date +%s%N)000000", "test log message"]]
    }
  ]
}' http://localhost:3100/loki/api/v1/push

# 使用curl查询Loki日志
curl http://localhost:3100/loki/api/v1/query_range?query={job%3D"example"}&start=$(date +%s -d "-1h")&end=$(date +%s)

# 使用Python客户端连接
# 安装客户端: pip install loki-client
python -c "
from loki_client import LokiClient
client = LokiClient(url='http://localhost:3100')
result = client.query('{job="example"}', time=int(time.time()), limit=10)
print(result)
"

# 使用Docker连接容器内Loki
# 注意：Loki容器内没有curl命令，使用wget替代
# cc

# 外部验证Loki服务状态
curl http://localhost:3100/ready
```

## 6. 日志管理

### 6.1 查看日志

#### 通过Docker命令查看
```bash
# 查看所有服务日志
docker-compose logs

# 查看特定服务日志
docker logs office_assistant_postgres

# 实时跟踪日志
docker logs -f office_assistant_redis

# 查看最新100行日志
docker logs --tail=100 office_assistant_rabbitmq
```

#### 通过Grafana查看（推荐）
1. 登录Grafana：http://localhost:3001
2. 点击左侧菜单「Explore」
3. 选择数据源为「Loki」
4. 使用LogQL查询日志，例如：
   - 查看所有日志：`{job="dockerlogs"}`
   - 查看特定服务日志：`{job="dockerlogs", container="office_assistant_postgres"}`
   - 查看错误日志：`{job="dockerlogs"} |= "error"`
   - 按时间范围查询：`{job="dockerlogs"} |~ "warning"` （配合时间选择器）

### 6.2 日志配置

日志配置位于`docker-compose.yml`文件中的每个服务下：
```yaml
logging:
  driver: "json-file"
  options:
    max-size: "10m"  # 单个日志文件最大10MB
    max-file: "5"     # 最多保留5个日志文件
    compress: "true"  # 自动压缩旧日志
```

## 7. 监控系统使用

### 7.1 Grafana配置

1. **登录Grafana**：http://localhost:3001（用户名：admin，密码：admin_password）

2. **添加数据源**：
   - **Prometheus**：
     - 名称：Prometheus
     - 类型：Prometheus
     - URL：http://prometheus:9090
     - 点击「Save & Test」
   - **Loki**：
     - 名称：Loki
     - 类型：Loki
     - URL：http://loki:3100
     - 点击「Save & Test」

3. **导入仪表盘**：
   - 点击左侧菜单「+」→「Import」
   - 输入仪表盘ID或上传JSON文件
   - 推荐仪表盘ID：
     - PostgreSQL：9628
     - Redis：11835
     - RabbitMQ：10991
     - Docker：13105

### 7.2 创建自定义仪表盘

1. 点击左侧菜单「+」→「Dashboard」
2. 点击「Add a new panel」
3. 选择数据源（Prometheus或Loki）
4. 使用查询编辑器创建查询
5. 配置图表样式和面板设置
6. 点击「Apply」保存

### 7.3 设置告警

1. 在仪表盘面板上点击「Alert」标签
2. 点击「Create Alert Rule」
3. 配置告警条件和阈值
4. 设置通知渠道（Email、Slack等）
5. 保存告警规则

## 8. 数据管理

### 8.1 数据备份

#### PostgreSQL备份
```bash
# 备份数据库
docker exec -t office_assistant_postgres pg_dump -U office_user office_assistant > backup.sql

# 备份压缩格式
docker exec -t office_assistant_postgres pg_dump -U office_user office_assistant | gzip > backup.sql.gz
```

#### Redis备份
```bash
# 执行Redis快照
docker exec -t office_assistant_redis redis-cli SAVE

# 复制备份文件
docker cp office_assistant_redis:/data/dump.rdb ./redis_backup.rdb
```

#### MinIO备份
```bash
# 使用MinIO客户端备份
docker run --rm -v $(pwd):/backup minio/mc mirror http://minio:9000/mybucket /backup/minio_backup
```

### 8.2 数据恢复

#### PostgreSQL恢复
```bash
# 恢复数据库
docker exec -i office_assistant_postgres psql -U office_user office_assistant < backup.sql

# 恢复压缩格式
gunzip -c backup.sql.gz | docker exec -i office_assistant_postgres psql -U office_user office_assistant
```

#### Redis恢复
```bash
# 复制备份文件到容器
docker cp ./redis_backup.rdb office_assistant_redis:/data/dump.rdb

# 重启Redis服务
docker restart office_assistant_redis
```

### 8.3 数据迁移

**跨主机迁移数据**：
1. 在源主机上备份数据（参考7.1节）
2. 将备份文件复制到目标主机
3. 在目标主机上恢复数据（参考7.2节）

**使用Docker卷迁移**：
```bash
# 源主机：导出卷
docker volume export ai-office-assistant_postgres_data > postgres_data.tar

# 目标主机：创建卷并导入
docker volume create ai-office-assistant_postgres_data
docker run --rm -i -v ai-office-assistant_postgres_data:/target alpine tar -xzf - -C /target < postgres_data.tar
```

## 9. 常见问题处理

### 9.1 服务启动失败
```bash
# 查看失败服务日志
docker-compose logs -f <服务名称>

# 检查端口冲突
docker ps # 查看已占用端口
netstat -tuln # 查看系统端口占用
```

### 9.2 连接被拒绝
- 检查服务是否正在运行：`docker-compose ps`
- 检查端口映射是否正确：`docker-compose port <服务名称> <内部端口>`
- 检查防火墙设置

### 9.3 数据卷权限问题
```bash
# 查看数据卷权限
docker run --rm -v ai-office-assistant_postgres_data:/data alpine ls -la /data

# 修复权限
docker run --rm -v ai-office-assistant_postgres_data:/data alpine chown -R 999:999 /data
```

### 9.4 服务响应缓慢
- 检查系统资源使用：`docker stats`
- 检查服务日志中的错误信息
- 考虑增加服务资源限制

## 10. 服务配置调整

### 10.1 调整服务资源

在`docker-compose.yml`中为服务添加资源限制：
```yaml
services:
  postgres:
    # ... 其他配置 ...
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 2G
        reservations:
          cpus: '0.5'
          memory: 1G
```

### 10.2 修改环境变量

在`docker-compose.yml`中修改服务的环境变量：
```yaml
services:
  postgres:
    # ... 其他配置 ...
    environment:
      - POSTGRES_USER=new_user
      - POSTGRES_PASSWORD=new_password
```

### 10.3 扩展服务实例

```bash
# 扩展服务实例数
docker-compose up -d --scale rabbitmq=2
```

## 11. 安全最佳实践

### 11.1 生产环境建议
- 修改默认密码
- 限制服务访问端口
- 启用TLS/SSL
- 定期备份数据
- 定期更新镜像
- 启用容器安全扫描

### 11.2 网络安全
```yaml
# 在docker-compose.yml中配置网络隔离
networks:
  office_network:
    driver: bridge
    internal: true  # 内部网络，不对外暴露
```

## 12. 更新与升级

### 12.1 更新服务
```bash
# 拉取最新镜像
docker-compose pull

# 重启服务以应用更新
docker-compose up -d
```

### 12.2 升级注意事项
- 备份数据前升级
- 检查服务版本兼容性
- 先在测试环境验证
- 升级后测试功能完整性

## 13. 故障排查工具

### 13.1 Docker内置工具
```bash
# 查看容器状态
docker inspect office_assistant_postgres

# 进入容器内部
docker exec -it office_assistant_postgres bash

# 查看容器资源使用
docker stats office_assistant_postgres
```

### 13.2 服务特定工具
```bash
# PostgreSQL诊断
docker exec -it office_assistant_postgres pg_stat_activity

# Redis诊断
docker exec -it office_assistant_redis redis-cli INFO

# RabbitMQ诊断
docker exec -it office_assistant_rabbitmq rabbitmq-diagnostics status
```

## 14. 监控指标参考

### 14.1 常用PromQL查询

#### PostgreSQL
```
# 连接数
pg_stat_database_numbackends{datname="office_assistant"}

# 查询速率
sum(rate(pg_stat_database_xact_commit{datname="office_assistant"}[5m])) + 
sum(rate(pg_stat_database_xact_rollback{datname="office_assistant"}[5m]))
```

#### Redis
```
# 内存使用
redis_memory_used_bytes

# 命中率
redis_keyspace_hits_total / (redis_keyspace_hits_total + redis_keyspace_misses_total)
```

#### RabbitMQ
```
# 队列长度
sum(rabbitmq_queue_messages_ready{queue=~"^office_.*"})

# 消息速率
sum(rate(rabbitmq_channel_messages_published_total[5m]))
```

## 15. 日志查询参考

### 15.1 常用LogQL查询

#### 基本查询
```
# 查看所有服务日志
{job="dockerlogs"}

# 查看特定服务错误日志
{job="dockerlogs", container="office_assistant_postgres"} |= "ERROR" | json

# 查看最近30分钟的警告日志
{job="dockerlogs"} |= "WARN" | json | __time__ > time() - 30m
```

#### 高级查询
```
# 统计错误日志数量
sum(count_over_time({job="dockerlogs"} |= "ERROR" [5m])) by (container)

# 查看慢查询日志
{job="dockerlogs", container="office_assistant_postgres"} |= "duration" | json | duration > 1000
```

## 16. 附录

### 16.1 常用命令速查表

| 功能 | 命令 |
|------|------|
| 启动服务 | `docker-compose up -d` |
| 停止服务 | `docker-compose down` |
| 查看状态 | `docker-compose ps` |
| 查看日志 | `docker-compose logs` |
| 重启服务 | `docker-compose restart` |
| 拉取镜像 | `docker-compose pull` |
| 清理资源 | `docker-compose down -v --remove-orphans` |

### 16.2 端口映射表

| 服务 | 容器端口 | 主机端口 |
|------|---------|---------|
| PostgreSQL | 5432 | 5432 |
| Redis | 6379 | 6379 |
| RabbitMQ AMQP | 5672 | 5672 |
| RabbitMQ Management | 15672 | 15672 |
| MinIO API | 9000 | 9000 |
| MinIO Console | 9001 | 9001 |
| Prometheus | 9090 | 9090 |
| Loki | 3100 | 3100 |
| Grafana | 3000 | 3001 |

## 16. 联系方式

如有问题或建议，请联系技术支持团队。

---

**文档版本**：1.0  
**更新日期**：2026-01-22  
**适用项目**：AI办公助手容器化部署